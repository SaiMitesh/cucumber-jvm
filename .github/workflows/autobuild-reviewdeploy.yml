name: CI/CD with Manual Approval

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "building......."

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: Prod
    steps:
      - name: mydepstep
        run: echo "dep done "
       
